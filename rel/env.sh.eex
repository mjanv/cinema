#!/bin/sh

# Distinct node name: Première Écoute already runs as `premiere_ecoute` on this
# host, and two releases sharing an sname would refuse to boot.
export RELEASE_DISTRIBUTION="sname"
export RELEASE_NODE="cinema"
