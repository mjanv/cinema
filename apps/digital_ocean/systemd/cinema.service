[Unit]
Description=Cinema Showtimes Phoenix Application
After=network.target

[Service]
Type=exec
User=cinema
Group=cinema
WorkingDirectory=/opt/cinema
EnvironmentFile=/opt/cinema/.env
Environment="MIX_ENV=prod"
Environment="LANG=en_US.UTF-8"
Environment="LC_ALL=en_US.UTF-8"

# No ExecStartPre: this app has no database and nothing to migrate.
ExecStart=/opt/cinema/bin/server

Restart=always
RestartSec=5
StartLimitIntervalSec=60
StartLimitBurst=3

StandardOutput=journal
StandardError=journal
SyslogIdentifier=cinema

# The droplet has ~2 GB with ~1 GB free, so this is roomy rather than tight.
# Measured ~113 MB at idle after warming 7 days of showtimes: an earlier 150M
# cap left it permanently above MemoryHigh, i.e. throttled by the kernel.
MemoryMax=300M
MemoryHigh=250M
MemorySwapMax=128M

CPUQuota=40%

# Security hardening
NoNewPrivileges=true
PrivateTmp=true
ProtectSystem=strict
ProtectHome=true
ReadWritePaths=/opt/cinema

[Install]
WantedBy=multi-user.target
